dependencies:
  pre: 
   - sudo apt-get update; sudo apt-get install g++; sudo apt-get install freeglut3-dev;

   
  post:
   - ls -ltr
   - g++ orbit.cpp -lGL -lGLU -lGLEW -lglut -o orbit
      
test:
  override:
  - ls -ltr orbit 
 
